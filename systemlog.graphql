query GetSystemLogs($first: Int!, $after: String) {
  systemLogs(first: $first, after: $after) {
    pageInfo {
      hasNextPage
      endCursor
    }
    nodes {
      id
      message
      severity
      userName
      createdAt
      source
      __typename
    }
  }
}
